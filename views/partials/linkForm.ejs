<div
  class="w-full max-w-3xl bg-gradient-to-br from-gray-800 to-gray-900 rounded-2xl shadow-2xl p-6 sm:p-10 text-center">
  <h1 class="text-2xl sm:text-3xl md:text-4xl font-bold text-purple-300 mb-4 leading-snug">
    Download any video from anywhere
  </h1>
  <p class="text-sm sm:text-base md:text-lg text-gray-300 leading-relaxed mb-8">
    Free online video downloader that supports most websites on the internet.
    Simple, fast, and secure — made to work across all devices.
  </p>

  <!-- Error Display -->

  <div id="errBox"
    class="hidden mb-6 w-full text-left bg-red-800/40 border border-red-600 text-red-300 rounded-lg p-4 space-y-2">
  </div>

  <!-- Styled Form -->
  <form class="flex flex-col sm:flex-row items-center gap-4" id="linkForm">
    <input type="text" name="videoLink" id="videoLink" required placeholder="Enter video URL"
      class="w-full sm:flex-1 px-4 py-3 rounded-lg bg-gray-700 text-white placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-purple-500" />
    <input type="submit" id="submitBtn" value="Start"
      class="w-full sm:w-auto px-6 py-3 bg-purple-600 hover:bg-purple-700 text-white font-semibold rounded-lg transition duration-200 cursor-pointer" />
    <!-- animation -->
    <div id="loadingSpinner" class="hidden flex items-center gap-2 text-purple-400 text-sm pl-1">
      <svg class="animate-spin h-5 w-5 text-purple-400" xmlns="http://www.w3.org/2000/svg" fill="none"
        viewBox="0 0 24 24">
        <circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" stroke-width="4"></circle>
        <path class="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8v4a4 4 0 00-4 4H4z"></path>
      </svg>
      Processing...
    </div>
  </form>
  <div id="downloadSection" class="hidden mt-8">
    <a id="downloadLink" href="#"
      class="inline-block px-6 py-3 bg-green-600 hover:bg-green-700 text-white font-semibold rounded-lg transition duration-200">
      ⬇️ Download Now
    </a>
  </div>
</div>